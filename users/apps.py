"""registering users(app) with django"""
from django.apps import AppConfig


class UsersConfig(AppConfig):
    """registering users(app) with django"""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'users'
